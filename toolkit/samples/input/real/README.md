# 本地脱敏真实语料

此目录用于可靠性回归，不提交任何原始业务文件。请在本机放入脱敏后的 `.docx`、`.xlsx`、`.pptx` 各至少 3 个；文件会被 `.gitignore` 排除。

每个文件应在本地记录：SHA-256、来源格式、触发步骤、预期结果、实际错误码、Windows/Office 版本，以及对应的公开最小复现 fixture。不要把原文、截图、Prompt、客户名称或业务数据写入测试报告。
