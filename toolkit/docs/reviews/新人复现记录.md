# 新人复现记录

复现日期:2026-07-10
复现视角:按公开文档从仓库根目录执行,不依赖模型密钥或内网服务
环境:macOS、Python 3.12、UTF-8

## 操作

1. 阅读 `README.md` 和 `docs/使用说明.md`。
2. 执行 `python scripts/record_demo.py`。
3. 检查 Word/Deck 两条命令退出码为 0,非法 DeckIR 命中 D003。
4. 执行 `PYTHONPATH=backend python -m pytest backend/tests -q`。
5. 执行 `python scripts/verify.py`。

## 结果

- CLI 参数与文档一致;无需 NGA token 即可运行 stub 链路。
- 成功/失败日志与截图位于 `samples/demo/`。
- 结构 expected 与 SHA-256 位于 `samples/expected/`。
- Windows wheel 文件已按 cp312/win_amd64 下载并记录 hash,但未在 Windows 断网安装。

自动复现结论:通过。此记录是交付可复现性预审,不冒充真实新员工或导师签收。
