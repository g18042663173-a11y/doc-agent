# 完整页仿版与便携发布

用户确认：源模板全部18页、原页序；保留截图像素；工作台只生成；不使用Docker。保留两Skill的分工和DeckIR 2.2/WordIR 1.3，模仿契约升为2.0。

源页、计划页、填充页和输出页完整对应。每槽记录材料证据。低匹配只指导修复，不能删页。缺页/槽、missing、无效引用、原生对象回读不一致及文字溢出不能通过正式验收。

普通文字、组合对象、表格、SmartArt、图表、可见共享文字使用关系与Content Types识别；图片字节不变。更新图表工作簿与缓存，SmartArt更新数据模型和显示缓存。保留参考源于内部工作目录供QA，不输出源文工作副本。

finalize状态为needs_visual_review；preview为内部draft。accept要求绑定最终PPTX哈希、全部页检查意见和导出PNG哈希，完成后accepted。任何输出修复必须重新生成报告与哈希。

真实材料使用完整72页中期报告，逐页引用物理PDF页码。实验表29与表17不同工作点分开；所有计算给出公式和单位，不编造日期、完成率或实验结果。

每个Skill ZIP携带独立Windows Python3.12、完整锁定依赖、启动与安装器。生成包含Graphviz，模仿含PDF、JSON Schema及真实Pydantic。三个短名ZIP、摘要、清单一次发布，隔离环境从实际ZIP验证。工作台只回归原生成流程。

验收必须包含全18页PowerPoint实际导出与逐页视觉复核、真实Office SmartArt独立fixture、图表数据/图片哈希回归、干净路径断网便携链路，以及跨Skill IR交接。

2026-09-05 排版纠正补充：有字、未越出文字框，不代表排版完成。逐页复核必须检查实际断行、字行间距、段距、长句挤压、表格上下留白，以及图表标签与侧栏。先缩写和按语义分行，再显式调整间距；表格按可读性调整对齐、内边距和字号，并记录相对源件的改变。新增 Office 行边界筛查只产生候选，不能把 `office_export.pass` 当作视觉通过。用户否定视觉结果时撤销原视觉结论，保留历史产物并生成新版本及对应证据。
