# Document Workbench 代码审查报告(第二轮 · 全仓)

日期：2026-08-12
范围：`backend/app`(约 2.1 万行 Python)+ `backend/tests`(656 passed / 15 skipped 基线全绿)+ `desktop/DocumentWorkbench`(C# WPF)+ `scripts/*` + PowerShell 打包/验证层
方法：8 个并行 subagent 深度静态审查(IR / parsers / rendering+template+visual / lint / generators+CLI / web_api+desktop_host / C# 桌面端 / scripts+打包),每条结论均对照真实代码路径;关键结论由主会话独立复现验证(见文末"人工复现记录")。
基线：`.venv` 下 `pytest backend/tests` = **656 passed, 15 skipped**——测试全绿但存在大量未覆盖路径上的 bug,说明测试缺口是本次发现的主要放大因素。

---

## 严重性统计(新增发现)

| 级别 | 数量 | 摘要 |
|---|---|---|
| CRITICAL | 2 | NGA CLI 进程树不终止；stub marker 碰撞导致静默内容丢失+崩溃 |
| HIGH | 9 | 主题路径穿越(library 层)；worker 线程崩溃永久停摆；md 13 列崩溃；UTF-16 实体扩展 DoS；BOM JSON 崩溃视觉 QA；manifest 缺 2 wheel；NGA 子进程管道不排空(复核#4)；等 |
| MEDIUM | ~30 | 见分节 |
| LOW | ~45 | 见分节 |

---

## 一、前轮报告(代码审查报告_2026-08-12.md)63 项结论核验

### 关键修正(与前轮结论不一致)

| # | 前轮结论 | 本轮判定 | 证据 |
|---|---|---|---|
| **#3** HIGH xlsx 同一 read-only sheet 迭代两次 | **FALSE**(锁定的 openpyxl 3.1.5 下) | `xlsx_parser.py:56,70,80-87,125-133` 确实传同一 `sheet` 给 `_preview_rows` 再 `_column_stats`;但 openpyxl 3.1.5 的 `ReadOnlyWorksheet.iter_rows` 每次调用都**重新解压并重新解析** worksheet XML(`_read_only.py:_cells_by_row` 每次 `with self._get_source()`),25 行表实测两趟结果都正确。残余代价只是 2-3 倍冗余解析(性能),不是错误数据。前轮"流耗尽/空迭代"结论不成立。 |
| **#26** LOW 运行时兜底改写 plan | **PARTIAL** | in-place 改写调用方 plan 属实(renderer.py:231-241);但磁盘 `template_plan.json` 在渲染循环之后才写(renderer.py:99-100),磁盘与实际一致。影响仅限共享对象污染,当前调用方无后果。 |
| **#27** LOW Presentation 不 close | **FALSE**(python-pptx 1.0.2) | 实测 `Presentation(path)` 加载时即整包读入内存并关闭 zip,函数返回即被引用计数回收,无"长期占用句柄"。旧版 python-pptx 才成立。 |
| **#38** LOW 非 docx 写 docx 风格文件名 | **PARTIAL** | docx_lint/pptx_lint 的报告文件名相同(`report.json`/`report.md`),"文件名"部分不成立;实质问题是 unsupported 后缀输出 `# DOCX 复检报告` 标题误导。 |
| **#13** LOW inline shape 尺寸 None | **REAL**(机制修正) | 不是 `None/914400`,是 python-docx 1.2.0 对缺失 `<wp:extent>` 抛 `MissingElementError`,实测缺 extent 的 docx 整个文件 E001 失败(触发点 `docx_parser.py:255`)。 |
| **#40** LOW chmod 0600 无效 | **REAL**(影响夸大) | state 文件只含 pid/port/version,**不含 session token**(bootstrap 文件才是敏感物且读完即删),泄露面实际只有端口/pid。 |
| **#56** C# Error 默认 new() | **PARTIAL** | 机制成立,但后端所有错误都走 `_error_response` 信封,非 JSON body 会走 JsonException 回退,当前触发面窄。 |
| **#57** Closing Dispose 竞争 | **PARTIAL** | 竞态真实但影响被高估:.NET 8 HttpClient.Dispose 取消在途请求,轮询 catch 都能接住,不会崩。 |

### 复核为 REAL 的前轮发现(47 项全部成立)

**CRITICAL/HIGH(全部 REAL)**:#1(NGA HTTP 配置桌面端永远保存失败——主会话已复现:payload 带 `transport`/`cli_path` → `NgaHttpConfig` `extra="forbid"` 报 `extra_forbidden`;且 C# `SettingsStore.cs:95-96` 恒序列化这两字段;`test_generator_settings_api.py:_nga_payload` 恰好不含这两字段掩盖了 bug)#2(main() 无 token 全 API 无鉴权,multipart simple request 无 CORS 预检)#4(C# 子进程管道从不排空)#5(主题名路径穿越,`rendering/theme.py:32-37`)#7(md 空表头崩溃)#28(超时/取消不终止线程与子进程)。

**MEDIUM/LOW 全部 REAL**:#6(UTF-16 绕过 DTD 扫描)、#8(md 吸行)、#9(多阈值标签重叠)、#10(Word 列表识别不到)、#11(空标题占位短路)、#14(docx 样式无存在性检查——当前默认模板下不可触发)、#15(`slide_layouts[6]` 硬编码——当前默认模板下可用)、#16(xlsx 首行隐藏)、#17(docx 严格 UTF-8 + 改写 theme1.xml)、#18(混合表索引整体 -1)、#19(shell 剥壳误判)、#20(首字符不换行)、#21(阈值永远用 `thresholds[0]`)、#22(DocumentIR 不校验 col_widths)、#23(容量评分用全部形状)、#24("XXXX" 审计空转)、#25(重绘清空品牌层)、#29(analyze 无超时)、#30(stderr 计入 10MB 上限)、#31(413 返回 HTML)、#32(命令行长度高估 2×)、#33(超时清理竞态)、#34(analysis-* 目录泄漏)、#35(_fail_job 锁外快照)、#36(word lint 不注册资产→404)、#37(codex 裸 AttributeError)、#39(取消/完成竞态 200)、#41(每次 diagnostics spawn dot)、#42(`Path("C:foo")` 绕过)、#43(lint 多序列拼接)、#44(check_pptx 无防御)、#45(mermaid `;`)、#46(负 tail_length)、#47(重名标题取首个)、#48(word/*.xml 严格 UTF-8)、#49(`M\d+` 误判)、#50(清单非原子)、#51(markdown 导出未转义)、#52(except Exception 吞错)、#53(嵌套工作簿整读内存)。

**C# 端 #54-#63 全部 REAL**(#56/#57 为 PARTIAL 见上);#63 补充:双实例会 `_recover_interrupted_jobs` 把对方运行中任务标失败**并删除其输入文件**,实为数据破坏而不仅是标失败。

---

## 二、IR 层新增发现

| # | 级别 | 位置 | 问题 |
|---|---|---|---|
| IR-N1 | MEDIUM | `ir/validation.py:274-354` | strict 模式未知字段收集**漏掉 image_text/image_grid/infographic** 嵌套结构;`image: {"focul_x": 0.9}` 拼错字段通过校验且被静默丢弃(拼错 focal 回退默认 0.5),repair 全走 strict 也发现不了 |
| IR-N2 | MEDIUM | `ir/common.py:90-95`、`deck_ir.py:153-158` | `col_widths` 只查 `width <= 0`,**NaN/Infinity 通过校验**(json.loads 默认接受 NaN)→ `Inches(nan)` 直达渲染器;chart 有 `math.isfinite` 检查,col_widths 没有 |
| IR-N3 | MEDIUM | `schemas/word_ir.schema.json` / `deck_ir.py:19` | 导出 Schema 与模型不一致:word classification 缺 enum;deck classification **模型层完全无校验**(Word 有枚举、Deck 无,同一概念约束不对称) |
| IR-N4 | MEDIUM | 导出 Schema | `DeckTable.rows`/`TableBlock.rows` 无 `minItems`(模型侧 `validate_rows` 要求 ≥1 行),Schema 门放行的空表被运行时拒绝→repair 回环 |
| IR-N5 | MEDIUM | `ir/shell.py:16` | `FENCED_JSON_RE` 非贪婪,code_block IR 字符串值含 ` ``` ` → fence 提前闭合→合法输出误报 truncated |
| IR-N6 | LOW | `ir/validation.py:107-109`、`shell.py:20` | 非 str/Mapping 输入抛裸 TypeError/AttributeError(仅捕获 JSONDecodeError),违反"失败给错误码"契约(当前调用方均传 str,低风险) |
| IR-N7 | LOW | `ir/validation.py:501-502` | `"level" in loc` 把 ListItem.level 错误映射为 E005(建议是 heading.level 的修复文案,误导) |
| IR-N8 | LOW | `ir/document_ir.py:97-105` | DocumentIR 表格不要求 rows 非空、image_placeholder 不要求 ref/caption,与 WordIR 契约不一致 |

疑点：1-based 启发式盲区(3 列表 start_col=2 时 evidence 不触发,静默按错列渲染)；`_category_start_index` 类别名逐字匹配；`_threshold_predicate` 只认"低于/高于/超过/超出"措辞;W101 标题层级归一化静默改写合法结构;`validate_document_ir` 无 strict 参数;repair 提示 `errors[:8]` 截断。

---

## 三、Parsers / Security 层新增发现

| # | 级别 | 位置 | 问题 |
|---|---|---|---|
| P-N1 | **HIGH** | `md_parser.py:194-198` | **md 表格 ≥13 列 → 整个文件 E001**(GFM 合法;docx/pptx 都有 12 列 cap,md 没有)✅主会话复现 |
| P-N2 | **HIGH** | `office_package.py:191-206` + `xlsx_parser.py:466+` | UTF-16 DTD 绕过字节扫描后,本仓库自己的 stdlib ElementTree 解析会**展开内部实体**(billion-laughs 内存 DoS 放大 #6,#6 从 MEDIUM 升 HIGH) |
| P-N3 | MEDIUM | `pptx_parser.py:82-87` | 回退标题形状未被 `_slide_bodies` 排除 → **标题文本重复进 bodies** |
| P-N4 | MEDIUM | `xlsx_parser.py:309-318` | 空 sheet 缺 `<dimension>` → openpyxl 内部 `NameError` → 整表 E001(只捕 ValueError) |
| P-N5 | MEDIUM | `docx_parser.py:255` | 任一 inline shape 缺失 `<wp:extent>`/docPr → 整个 docx 解析失败(pptx 有 `_emu_to_inches` try/except 保护,docx 没有) |
| P-N6 | LOW | `xlsx_parser.py:56-59` | 两个 workbook 在 try/finally 之外打开,metadata 扫描抛错时泄漏文件句柄(Windows 锁源文件) |
| P-N7 | LOW | `md_parser.py:197` | `x\|y` 转义管道被切 3 段 → 行丢弃(数据丢失) |
| P-N8 | LOW | `md_parser.py:174-191` | 第二个分隔行 `---|---` 被当数据行 |
| P-N9 | LOW | `docx_parser.py:234` | UTF-16 部件 `errors="replace"` 解码 → unsupported 特征扫描对 UTF-16 文件恒漏报 |
| P-N10 | LOW | `pptx_parser.py:253-267` | 非连续 slide 编号(删除过页)时 embedding 警告挂错 slide |
| P-N11 | LOW | `md_parser.py:155-163` | UTF-16 md 走 gb18030 兜底变乱码且无错误 |
| P-N12 | LOW | `xlsx_parser.py:254-255` | 谎报 `<dimension>` 构建 ~1M 元素列表 + nrows 虚高 |
| P-N13 | LOW | `docx_parser.py:137` | `w:outlineLvl val="-1"` → level 0 → ValidationError → E001(>4 有 cap,负数没有) |

疑点：md 内联 markdown 不剥离(`**bold**`/`[link]` 原样进 IR)；docx 合并单元格不填充(与 xlsx `_fill_merged_preview` 不一致);`notes_slide` 为只读 pass 物化 notes;`_drawing_anchor_label` 无 try/except;安全拒绝映射为 E003(格式码)误导;≥10MB xlsx 子进程路径硬失败 vs 进程内优雅降级不对称。

---

## 四、Rendering / Template / Visual 层新增发现

| # | 级别 | 位置 | 问题 |
|---|---|---|---|
| R-N1 | MEDIUM | `template/renderer.py:71` | plan 与 deck 页数/版本不一致**静默截断**;版本从不比对;唯一防线是渲染完成后的裸 ValueError |
| R-N2 | MEDIUM | `template/package.py:54` | `startswith("ppt/slides/slide")` 把 **slideLayout 计入页数** → 合法模板(1 页+200 版式)被误拒 |
| R-N3 | MEDIUM | `pptx_renderer.py:882-894` | combo 图阈值被 `model_copy(thresholds=[])` **静默丢弃**,但 IR 校验允许 combo+thresholds,侧结论文字还在 |
| R-N4 | MEDIUM | `pptx_renderer.py:1087-1090` vs `hw_theme.json:342` | 主题键 `label_top_offset_in` 定义但从未被读取(与 #9 同根,标签定位契约漂移) |
| R-N5 | MEDIUM | `diagram/mermaid_diagram.py:170` | 节点标签含 `-->` → 整图报废(与 #45 的 `;` 同类);文本模式 pipe 与 architecture_graphviz 二进制模式策略不一致 |
| R-N6 | MEDIUM | `pptx_renderer.py:1835-1846` | 纵向 7 步卡片高仅 ~0.55in,内容必然溢出且无字号拟合 |
| R-N7 | LOW | `pptx_renderer.py:534-551` | tcPr 子元素顺序 solidFill 在 lnL 前,违反 OOXML 模式顺序 |
| R-N8 | LOW | `docx_renderer.py:192-204,468-482` | pPr 子元素顺序 shd 在 pBdr 前,违反模式顺序 |
| R-N9 | LOW | `pptx_renderer.py:1408` | graphviz 节点缺失时裸 KeyError,无确定性布局回退(只检查了边) |
| R-N10 | LOW | `pptx_renderer.py:1272-1273` | `_visual_text_length` 不识增补平面 CJK/emoji(代理对按 1 计) |
| R-N11 | LOW | `pptx_renderer.py:1698-1707,2146-2149` | 连接器 shape.name 内嵌原始节点 id(含 `:`/空格 → 语义歧义) |
| R-N12 | LOW | `template/renderer.py:72` | `prototype_index` 越界 → 裸 IndexError |
| R-N13 | LOW | `template/theme.py:31` | 缺 accent2-6 时全部回落 accent1 → 多序列同色 |
| R-N14 | LOW | `rendering/theme.py:11-16,32-33` | `THEME_REGISTRY["hw_v1"]="hw_v1"` 但 `load_theme("hw_v1")` 读 `hw_theme.json`;**修 #5 时必须同步该映射,否则默认主题 FileNotFoundError** |
| R-N15 | LOW | `docx_renderer.py:494-504` | `.tmp.docx` 临时文件无 finally 清理,失败时产物"看似成功" |

疑点：饼图系列色走 spPr 层、PowerPoint 按数据点(dPt)着色,扇区色可能不生效;combo 双网格线;composite 探测高度与实际不一致;`_render_agenda` 无拟合;封面/章节页也渲染密级页码(需人确认);profile.py `getattr(shape,"left",0)` 对 None xfrm TypeError;`tblW` 未设置;`_write_sequence_text` MIDDLE 锚定溢出。

---

## 五、Lint 层新增发现

| # | 级别 | 位置 | 问题 |
|---|---|---|---|
| L-N1 | MEDIUM | `docx_lint.py:119-125` | E004"文档未使用主题红"对**工具链自身产物恒为真空规则**——渲染器改写 theme1.xml 必然含 C7000B;实测把 heading 全改蓝仍 lint 全绿;`4F81BD` 检查同样被渲染器抵消 |
| L-N2 | MEDIUM | `pptx_lint.py:152`、`docx_lint.py:274-277` | 只看 latin 字体,a:ea / w:eastAsia 字体白名单违规完全不可见(两侧对称漏检) |
| L-N3 | MEDIUM | `pptx_lint.py:952-962` | 合并单元格溢出估计只用锚点行列尺寸 → 水平合并被误报 HW-W03(渲染器自身就产合并单元格) |
| L-N4 | LOW | `pptx_lint.py:984-985` | 非 normAutofit 文本框溢出规则整体 no-op(外部文件复检失效) |
| L-N5 | LOW | `docx_lint.py:135-136,263-271` | 超链接 run(w:hyperlink>w:r)与文本框内容(txbxContent)完全不检查 |
| L-N6 | LOW | `pptx_lint.py:1341-1345` | bottom 落在 7.0-7.55" 即判页脚 → 正文 tall 文本框整块豁免 |
| L-N7 | LOW | `pptx_lint.py:1407-1421` | 任何含"目录"文本的页都被当 agenda 页;单数字章节号不计数 → 误报 HW-I01 |
| L-N8 | LOW | `docx_lint.py:157-164` | 表头/边框是整行/整表级存在性检查,单个白底单元格混入不报 |
| L-N9 | LOW | `docx_lint.py:249-250` | xml:space 段落级存在性检查,首 run 丢失不报 |
| L-N10 | LOW | `docx_lint.py:320-321` | docx lint 无 W 层,所有规则强制 Error;字号边界 docx 精确等于 vs pptx ≥ 下限,不对称 |
| L-N11 | LOW | `docx_lint.py:108-114` | 全量解码拼接 3-4 份驻留内存;任一部件非 UTF-8 整包 E001 |
| L-N12 | LOW | `docx_lint.py:223-224` | 每个段落无条件 `paragraph._p.xml` 序列化,仅两种样式需要 |
| L-N13 | LOW | `pptx_lint.py:538-561` | "累计高度"实际是最大 bottom−content_top,数值虚高;顶部越界不检测 |
| L-N14 | LOW | `pptx_lint.py:441-468` | 无图页上的竖置阈值线误报方向不一致 HW-W07 |
| L-N15 | LOW | `pptx_lint.py:70`、`docx_lint.py:53` | lint 期间产物文件被 ZipFile 句柄锁住(Windows 与清理任务竞态) |

疑点：combo 次轴取第一个 valAx;19pt 标题阈值误判外部文件;版权文本精确匹配;`buNone` run 级判定;CJK 扩展 B 计数不一致;空段落计一行;多节链接页脚行为未实测。

---

## 六、Generators / Generation / Prompting / Assets / CLI 层新增发现

| # | 级别 | 位置 | 问题 |
|---|---|---|---|
| G-N1 | **CRITICAL** | `nga.py:494-498` | **NGA CLI 进程树从不 kill**：CLI 启动器 spawn 子进程后,超时/输出超限只杀直接子进程,孙进程存活并持有管道 → drain 线程永久阻塞 + 每次重试再 spawn 新进程;Windows 无 CREATE_NO_WINDOW,可能闪控制台窗口 |
| G-N2 | **HIGH** | `rendering/theme.py:32-37`(library 层) | 主题路径穿越只在 web API 被 `_theme_from_form` 缓解;**CLI 与库调用(render/check/prompt builder)不受保护**,任意本地 .json 内容原样注入 LLM prompt → 外带通道 |
| G-N3 | MEDIUM | `stub.py:51-60` + `depth.py:228` / `analysis.py:86` | **stub marker 碰撞**:`rfind` 取最后出现;`[输入 DocumentIR]`/`[Deck 大纲规划]`/`[分析实测数据]` 三个 marker 都在用户内容**之前**注入,用户文档含该串 → 解析 None → 静默 placeholder(内容丢失)或 ValueError 崩溃 ✅主会话复现 |
| G-N4 | MEDIUM | `cli/analyze.py:46` | 只捕 RuntimeError,stub 的 ValueError 裸 traceback 逃逸,无 E/W/D 封套 |
| G-N5 | MEDIUM | `ir/repair.py:49-56` | repair 提示词不含任何 marker → stub 无法从 repair 提示提取上下文,重试必失败直到 D006,真实原因被掩盖 |
| G-N6 | MEDIUM | `nga.py:27-33,288-298` | NGA **CLI 传输在 Windows 上无法生成 deck**(argv 32767 上限 vs ~52KB DeckIR schema),目标平台必 E010,而 UI 把 CLI 当一等传输 |
| G-N7 | MEDIUM | `codex.py:167-177,101,82-85` | chat_completions 模式丢 max_tokens;响应无上限读取;无重试(与 NGA 不对称) |
| G-N8 | LOW | `stub.py:287-313` | visual plan 只应用第一个机会,其余静默丢弃 |
| G-N9 | LOW | `stub.py:118,321,469`、`depth.py:818` | 裸 `int()` 对非数字 level/index 崩溃(防御性) |
| G-N10 | LOW | `codex.py:16-17` | 默认 `gpt-5.6-terra`/`xhigh` 是不存在的值,无 env 时对真端点必 400 |
| G-N11 | LOW | `web_api.py:676-681` | 幂等键重放绕过限流 |
| G-N12 | LOW | `builder.py:552-557` | #46 补充:51≤limit≤54 时 `tail_length≤0`,`text[-0:]` 返回全文同样超限(当前调用方均 ≥120,潜伏) |

疑点：`_few_shot` 依赖源树 `samples/ir`(打包必须带);NGA CLI NDJSON 契约无离线 fixture;codex CLI 的 `-s read-only` 可能是 session 标签而非 sandbox;深度算术无测试钉住;分块 xlsx/pptx 全文重嵌、每块独立截断不确定。

---

## 七、Web API / Desktop Host 层新增发现

| # | 级别 | 位置 | 问题 |
|---|---|---|---|
| W-N1 | **HIGH** | `web_api.py:340-384` | **`JobRunner._loop` 无任何 try/except**：超时瞬间磁盘满/权限错 → `_fail_job` 抛 OSError → worker 线程死亡 → 队列永久停摆、pending 永不回收(expires_at 只在 terminal 设置)、slot 永不释放(新请求持续 429)、无 worker 监督重启 |
| W-N2 | MEDIUM | `web_api.py:340-384` | 任务**严格串行**(单 worker 阻塞在 finished.wait),`queue_capacity=4` 只是接受上限,与 health/UI 宣称的并发语义矛盾;第 5 个任务最多等 ~1 小时 |
| W-N3 | MEDIUM | `web_api.py:363-383` | 超时/取消后 slot 立即放行、新任务即刻启动,遗留"僵尸"任务线程线性累积(与 #28 叠加) |
| W-N4 | MEDIUM | `web_api.py:583-589` | `/api/settings/generator/test` 同步执行、无超时护栏,HTTP 传输最坏 ~45 分钟占死 waitress 4 线程之一(放大 #29) |
| W-N5 | MEDIUM | `web_api.py:944` vs waitress | waitress 默认先整包缓冲 1GB 再触发 Flask 413,4 线程并发内存峰值 ≥1GB;建议 waitress `max_request_body_size` 与 Flask 对齐 |
| W-N6 | LOW | `web_api.py:829-905` | **无 `/` 路由**：浏览器模式访问 `http://127.0.0.1:5056/` 404;静态缓存 12h,前端更新后浏览器继续旧版 |
| W-N7 | LOW | `web_api.py:1088` | chunked 请求 `content_length=None` → 磁盘预检只要求 512MB,与真实占用偏差可达 3×260MB |
| W-N8 | LOW | `web_api.py:790-813` | 取消路径 `_fail_job` 写盘失败抛裸异常 → 500 HTML 破坏封套 |

疑点：watchdog `server.close()` 跨线程调用未实测;`version.py parents[2]/VERSION` 打包形态下必失败;`except ValueError` 接不住 `queue.Full`(理论不可达);state 文件 replace 前无 fsync;deck+CLI 硬拒绝与 analyze 路径策略分裂。

---

## 八、C# 桌面端新增发现

| # | 级别 | 位置 | 问题 |
|---|---|---|---|
| C-N1 | MEDIUM | `MainWindow.xaml.cs:138-144,489` | **两个并发轮询循环竞争 `_currentJob`/`_busy`/进度面板**：启动恢复任务(未 SetBusy)后用户点生成 → 旧任务终态时旧循环 `SetBusy(false)`/`ShowJob` 覆盖新任务 UI,可重复提交 |
| C-N2 | MEDIUM | `MainWindow.xaml.cs:512-517` | 终态任务的 `RefreshJobsAsync` 在 try 内,抛 HttpRequestException → 进入无限重试分支,任务已终态 → 死循环空转 |
| C-N3 | MEDIUM | `BackendProcessHost.cs:125` | 全局 HttpClient 超时 20 分钟,700ms 轮询受其约束——后端"挂死"时单请求卡 20 分钟、Cancel 也排队 |
| C-N4 | MEDIUM | `MainWindow.xaml.cs:867-878` | NGA 保存**本地先写、后端后验**：HTTP 配置必 E010,但 token 已进凭据管理器、settings 已持久化 → 配合 #1 每次启动 E010 → BlockNga 永久阻断 |
| C-N5 | LOW | `ApiModels.cs:299-318` | StageLabels 缺 `timed_out/interrupted/falling_back_to_stub/canceling/analyzing`,失败定位显示英文裸串 |
| C-N6 | LOW | `MainWindow.xaml.cs:1174` | `FormatBytes(0)` 显示 "1 KB" |
| C-N7 | LOW | `WorkbenchApiClient.cs:119` | 下载临时文件固定名 `.download`,并发下载冲突+进程被杀残留 |
| C-N8 | LOW | `BackendProcessHost.cs:253-260` | 强杀后含 session token 的 bootstrap 文件滞留磁盘最长 10 分钟 |
| C-N9 | LOW | `MainWindow.xaml.cs:630` | 重试按钮用的是**当前**输入而非所选任务输入,误导 |
| C-N10 | LOW | `MainWindow.xaml.cs:683-691` | 任务列表刷新丢 DataGrid 选中项,OpenSelectedTask 静默无响应 |

疑点：双击生成连击(SetBusy 在 try 外);CA+不校验 TLS 组合无 UI 预校验;Fallback 状态从不显示;NTFS ACL 依赖(FAT 卷启动失败);启动期 NGA 测试阻塞 Window_Loaded 至多 120s。

---

## 九、Scripts / 打包 / 验证层新增发现

| # | 级别 | 位置 | 问题 |
|---|---|---|---|
| S-N1 | **HIGH** | `scripts/office_visual_export.ps1:71-72` + `ppt_visual_qa.py:97-99` | PS 5.1 `-Encoding utf8` 恒写 **BOM** → `json.loads(utf-8)` 必抛 JSONDecodeError;**目标机(pwsh 缺失时)整个 Windows 视觉 QA 门禁 100% 崩溃** ✅主会话已确认 PS5.1 写 BOM 行为 |
| S-N2 | **HIGH** | `MIGRATION_PACKAGE_MANIFEST.json` | **缺 `colorama` 与 `waitress` 两个 wheel**(wheelhouse 29 个 vs manifest 27+2);按快照交付后 `--require-hashes -r lock` 离线安装必失败;`package_windows_dev.py` 打包不校验 wheelhouse↔lock ✅主会话复现 |
| S-N3 | MEDIUM | `package_document_workbench.py:132-144` | 断点续传:服务器忽略 Range 返回 200 → 全量**追加**到残缺文件 → 哈希校验失败但不删损坏缓存 → 永久失败 |
| S-N4 | MEDIUM | `stop_workbench.ps1:35-44` | 运行中的子进程(dot.exe、NGA CLI)触发 `throw` → 停服中止,进程与 PID 文件残留 |
| S-N5 | MEDIUM | `ppt_visual_qa.py:97` / `html2pptx_benchmark.py:238` | 忽略子进程退出码,凭旧报告文件存在性判 pass → 失败可能误判成功 |
| S-N6 | MEDIUM | `visual_review_shots.py:96-99` | 120ms 后截图,stub 生成太快 → 捕获到的是成功态而非进度态 |
| S-N7 | MEDIUM | `build_delivery_assets.py:226,263,288` | 依赖先手工跑 make_dirty_samples.py,缺前置校验 → 裸 FileNotFoundError+半成品 manifest |
| S-N8 | LOW | `verify.ps1:13` / `bootstrap_windows.ps1:32` | `$null.Trim()` 抛误导性错误 |
| S-N9 | LOW | `make_wheelhouse.py:24-38` | 未钉 `--implementation cp --abi cp312`,开发机非 3.12 会下载错 ABI |
| S-N10 | LOW | wheelhouse | **ruff 不在离线 wheelhouse** → 内网机器静态检查门禁不可运行 |
| S-N11 | LOW | `contact_sheet.py:52` | PIL 默认字体无 CJK 字形,中文页名渲染为方块 |
| S-N12 | LOW | `hash_wheelhouse.py:71` | 缺 METADATA 的 wheel 抛裸 StopIteration |
| S-N13 | LOW | `package_document_workbench.py:266-273` | 生产运行时附送 pytest/coverage;按目录名删所有 `test`/`tests` 目录过激 |
| S-N14 | LOW | `package_installer.py:56-59` | 无条件 rmtree 同名目录(非本脚本产物被删) |
| S-N15 | LOW | `demo_e2e.py:56-64` | 参数错误发生在 document_ir.json 写出之后,错误路径留下半成品 |
| S-N16 | LOW | `start_workbench.ps1:56` | 手工引号拼接,路径含 `"` 时命令行破坏 |
| S-N17 | LOW | `package_document_workbench.py:181-213` | 离线 restore 依赖预置 .packages,缺失任何包会尝试联网失败,无完整性校验 |

疑点：visual_diff 阈值对 Office 版本/字体 antialias 敏感;`manual_pending` 返回码 0 语义;`networkidle` 与 700ms 轮询冲突超时;`--skip-office` 使硬门禁恒真;`python312._pth` 加载用户 site-packages;verify.py coverage.json 无防护;installer.iss 手工运行版本漂移+不清旧目录。

---

## 十、跨层结论与修复优先级

### 结构性观察

1. **测试全绿但真实 bug 大量存在**(656 passed/15 skipped)——掩蔽因素：NGA 测试 payload 恰好不含 `transport/cli_path`(#1)；pptx 测试只测单阈值(#9/IR-N)；lint 测试样本都是渲染器自产文件(命中 L-N1 的 theme1.xml 改写抵消);md 测试无 13 列/转义管道用例;无 marker 碰撞用例;无 stub repair 用例。
2. **验证器"放行语义错误数据"比绕过验证更危险**:#18(整表索引 -1)、#21(阈值取错)、IR-N2(NaN)、IR-N1(拼错字段静默丢弃)都让"校验通过"的 IR 携带错误语义直达渲染器,符合 I1 的"壳"但违背"数据正确"。
3. **Windows 平台特有的三类问题贯穿全仓**:UTF-16 编码绕过(P-N2/L-N11/docx_renderer #17)、cmdline 32767 限制(G-N6/#32)、文件句柄锁定(N15/C-N8/#33)。
4. **marker/rfind 类"文本协议"解析是本仓最脆弱的模式**:stub marker 碰撞(G-N3)、shell 剥壳(#19/IR-N5)、md 表格吸收(#8)同根。

### 建议修复顺序

1. **立即修(阻断核心功能/安全)**:#1+C-N4(NGA HTTP 配置全链路不可用且启动即阻断)、G-N1(CLI 进程树)、G-N2(主题穿越 library 层)、#2(API 无鉴权)、#4(管道排空)。
2. **本周修(数据错误/崩溃)**:IR-N2(NaN)、#18(表索引)、G-N3(碰撞→静默丢内容)、P-N1(13 列)、P-N2(UTF-16 DoS)、W-N1(worker 崩溃停摆)、L-N1(主题红真空规则)。
3. **排期修**:S-N2(manifest 缺 wheel,打包前加 lock↔wheelhouse 校验)、S-N1(BOM)、#28/W-N3(线程终止)、#29/W-N4(test/analyze 超时)、C-N1/C-N2/C-N3(轮询三兄弟)。
4. **回归验证**:修 #1 前先给 `test_generator_settings_api.py` 的 payload 补上 `transport/cli_path`;修 #9/IR-N5 时补多阈值/``` 用例;修 L-N1 时补"heading 全蓝仍应报 E004"用例。

---

## 附:主会话人工复现记录

| 结论 | 复现方式 | 结果 |
|---|---|---|
| #1 NGA HTTP 配置失败 | `NgaHttpConfig.model_validate({...,'transport':'http','cli_path':'nga'})` | 拒绝,错误恰好 `[(('transport',),'extra_forbidden'),(('cli_path',),'extra_forbidden')]`;去掉两字段后通过(失败仅因测试 payload 的 verify_tls 组合) |
| S-N2 manifest 缺 wheel | 解析 `MIGRATION_PACKAGE_MANIFEST.json` vs `glob wheelhouse/*.whl` | 29 个实际 wheel 中 `colorama`、`waitress` 不在 manifest(其余 27 个在) |
| P-N1 md 13 列崩溃 | `parse_markdown` 喂 13 列表格 | `E001 无法读取或解析输入文件`(13 列表格杀死整个文件) |
| G-N3 marker 碰撞 | 构造含 `[输入 DocumentIR]` 的上下文 prompt | `_extract_document_context` 返回 None(→ 静默 placeholder);大纲 marker 同样 None(→ ValueError) |
| 测试基线 | `.venv\python -m pytest backend/tests` | 656 passed / 15 skipped,全绿 |
| PS5.1 BOM | 本机 PS 5.1 实测 `-Encoding utf8` | 恒写 `EF BB BF` |

(注:所有断言均可复跑;临时脚本位于 `%TEMP%\opencode\verify_*.py`,未触碰仓库文件。)
